import React from 'react';
import PropTypes from 'prop-types';
import { StyledHeroImage } from 'styles/HeroStyle';

const Hero = props => {
    return (
        <StyledHeroImage>
            Hero
        </StyledHeroImage>
    );
};

Hero.propTypes = {
    
};

export default Hero;