import React from 'react';
import PropTypes from 'prop-types';
import { StyledMovieThumb } from 'styles/MovieThumbStyle';

const MovieThumb = props => {
    return (
        <StyledMovieThumb>
            moviethumb
        </StyledMovieThumb>
    );
};

MovieThumb.propTypes = {
    
};

export default MovieThumb;